/// @file config.cpp
/// @brief Configuration implementation (mostly in header, this provides explicit instantiation)

#include <structor/config.hpp>

namespace structor {

// Explicit template instantiations if needed in the future
// Currently the implementation is header-only

} // namespace structor
